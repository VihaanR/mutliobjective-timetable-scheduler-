[pytest]
testpaths = tests
# third_party holds standalone scripts, not tests; smoke_test.py matches *_test.py
# and must never be collected.
norecursedirs = third_party .venv-fork .venv-baseline .dump or-tools-reference
